<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="uic.hcilab.mciapp">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SelectionActivity"></activity>
        <activity android:name=".SpeechActivity" />
        <activity android:name=".KeyboardActivity" />
        <activity android:name=".AfterHandwritingActivity" />
        <activity android:name=".AfterPatternActivity" />
        <activity android:name=".AfterZoomActivity" />
        <activity android:name=".AfterAlbumActivity" />
        <activity android:name=".ZoomActivity" />
        <activity android:name=".HandwritingActivity" />
        <activity android:name=".PatternActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AlbumActivity" />
        <activity android:name=".EndActivity" />

        <activity
            android:name=".ErrorActivity"
            android:label="@string/app_name"
            android:launchMode="singleTask"
            android:screenOrientation="nosensor" >
        </activity>
    </application>

</manifest>